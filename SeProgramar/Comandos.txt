Gobstones:

program{}
	Mover()	opuesto() previo() siguiente()
	Poner()	
	Sacar()
	IrAlBorde()
	repeat() {}
	nroBolitas(color)	hayBolitas(color)
	if(){	else ()	}
	puedeMover(direccion)
procedure Nombre() {
	Acciones
}
function Nombre() {return()	}
El operador && que sirve para tomar
dos expresiones booleanas y devolver True solo si
ambas son verdaderas,
La disyunción, que se escribe || y retorna verdadero
si al menos alguna de las dos preguntas es verdadera.

JavaScript:

x += y; //equivalente a x = x + y;
x *= y; //equivalente a x = x * y;
x -= y; //equivalente a x = x - y;
x++;    //equivalente a x = x + 1;

Igualdad  ===	


Ruby:


comparacion  ==




